#include "main.h"
/**
 * _isupper - uppercase letters
 * @c: character to inspect
 *
 * Return: Uppercase character? return 1 or else return 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')

	return (1);

else

	return (0);

}
